/**
 * author: zhanghuan
 * created: 2020/3/30
 * describe: 测试的接口
 */
const { prefix, mockPrefix } = window._CONFIG

export default {
    getData: `${prefix}/openapi/v1/migration/rank`,
    menu: `${mockPrefix}/menu`,
}